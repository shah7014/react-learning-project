import { Box, styled } from "@mui/material";

export const MovieBox = styled(Box)(({ theme }) => ({
  backgroundColor: "#2b3035",
  borderRadius: "0.9rem",
  overflow: "auto",
  height: "600px",
}));
